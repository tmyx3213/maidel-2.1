# Claude API Configuration
CLAUDE_API_KEY=your_claude_api_key_here
CLAUDE_API_URL=https://api.anthropic.com

# MCP Server Configuration
MCP_GOOGLE_CALENDAR_SERVER_PATH=npx @cocal/google-calendar-mcp

# Development Configuration
NODE_ENV=development
LOG_LEVEL=info

# Google OAuth (stored securely in keychain, not in .env)
# GOOGLE_OAUTH_CREDENTIALS will be managed by keytar